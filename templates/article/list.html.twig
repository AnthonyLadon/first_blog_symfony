{% extends 'base.html.twig' %}

{% block title %}Listes de chiffres bidon{% endblock %}

{% block body %}
<div class="container">
    <h1 scope="row">Mon super blog</h1>
    <p>Exercice: Créer un tableau de nombre aléatoires, ceux-ci seront affiché de manière à séparer les nombres paires des nombres impaires
    </p>

    <div>
        <h3> {{titre_nb}}</h3>
        <table class="table table-striped table-hover">
            <th class="table-primary">pairs</th>
            {# Triage les chiffres pairs #}
                {% for item in tab_nombres|sort %}
                    {% if item is even %}
                    <tr>
                        <td>{{item}}</td>
                    </tr>
                    {% endif %}
                {% endfor %}
            <th class="table-primary">impairs</th>
            {# Triage les chiffres impairs #}

                {% for item in tab_nombres|sort %}
                    {% if item is odd %}
                    <tr>
                        <td>{{item}}</td>
                    </tr>
                    {% endif %}
                {% endfor %}
        </table>
    </div>
    <div class="row align-items-start">
        <div class="col">
            <p class="p-2"> Exercice: Afficher le plus grand nombre de votre tableau de nombre:</p>
            <p class="p-2">{{ max(tab_nombres) }}</p>
        </div>
        <div class="col">
            <p class="p-2"> Exercice: Afficher aléatoirement une des valeurs de mon tableau:</p>
            <p class="p-2">{{ random(tab_nombres)}}</p>
        </div>
    </div>
</div>
{% endblock %}
